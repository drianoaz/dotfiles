#!/usr/bin/env bash

git config --global user.name "Adriano de Azevedo"
git config --global user.email "contato@adriano.dev"

git config --global init.defaultBranch main
git config --global push.default current